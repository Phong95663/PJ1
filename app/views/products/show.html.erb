<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p><strong>Existing Images:</strong></p>


<%= form_for [@product, Image.new], :html => {id: 'image-dropzone', class: :dropzone, multipart: true} do |f| %>
  <%= f.hidden_field :image %>
<% end %>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
<%= javascript_include_tag "admin/dropzone_config" %>